query($ident: ID!, $historikk: Boolean = false) {
    hentIdenter(ident: $ident, historikk: $historikk) {
        identer {
            ident
            historisk
            gruppe
        }
    }
}
